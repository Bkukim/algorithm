package org.exhaustive_search;

/**
 * packageName : org.exhaustive_search
 * fileName : PrimeNumbers
 * author : PC
 * date : 2024-09-13
 * description :
 * 요약 :
 * <p>
 * ===========================================================
 * DATE            AUTHOR             NOTE
 * -----------------------------------------------------------
 * 2024-09-13         PC          최초 생성
 */
public class PrimeNumbers {
    // 1. 먼저 모든 숫자를 다 만든다.
    // 2. 그 수가 소수인지 확인한다.
    // 3. 소수이면 count 를 증가시킨다.
    public int solution(String numbers) {
        int answer = 0;
        for (int i = 0; i < numbers.length(); i++) {
            for (int j = 0; j < ; j++) {

            }
        }
        return answer;
    }
}
